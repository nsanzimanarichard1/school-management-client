# System Architecture

## Application Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                     SCHOOL MANAGEMENT SYSTEM                     │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐         ┌──────────────────────────┐
│   CLIENT APPLICATION     │         │   ADMIN APPLICATION      │
│   (Port 5173)            │         │   (Port 5174)            │
│                          │         │                          │
│  ┌────────────────────┐  │         │  ┌────────────────────┐  │
│  │  Login/Register    │  │         │  │  Admin Login       │  │
│  └────────────────────┘  │         │  └────────────────────┘  │
│           │              │         │           │              │
│           ▼              │         │           ▼              │
│  ┌────────────────────┐  │         │  ┌────────────────────┐  │
│  │  Dashboard         │  │         │  │  Dashboard         │  │
│  │  - Fee Balance     │  │         │  │  - Statistics      │  │
│  │  - Attendance      │  │         │  │  - Overview        │  │
│  └────────────────────┘  │         │  └────────────────────┘  │
│           │              │         │           │              │
│  ┌────────┴────────┐     │         │  ┌────────┴────────┐     │
│  │                 │     │         │  │                 │     │
│  ▼                 ▼     │         │  ▼                 ▼     │
│ ┌──────┐      ┌────────┐│         │┌──────┐      ┌────────┐  │
│ │ Fees │      │Academic││         ││Users │      │Classes │  │
│ └──────┘      └────────┘│         │└──────┘      └────────┘  │
│                          │         │      │            │      │
└──────────┬───────────────┘         └──────┼────────────┼──────┘
           │                                │            │
           │                                │            │
           └────────────────┬───────────────┴────────────┘
                            │
                            ▼
                ┌───────────────────────┐
                │   BACKEND API         │
                │   (Port 5000)         │
                │                       │
                │  ┌─────────────────┐  │
                │  │  Authentication │  │
                │  │  - JWT Tokens   │  │
                │  │  - SHA-512      │  │
                │  └─────────────────┘  │
                │           │           │
                │  ┌────────┴────────┐  │
                │  │                 │  │
                │  ▼                 ▼  │
                │ ┌────┐         ┌────┐│
                │ │ DB │         │Auth││
                │ └────┘         └────┘│
                └───────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │   DATABASE    │
                    │   (MongoDB)   │
                    └───────────────┘
```

## Component Architecture

### Client Application Structure
```
client-app/
│
├── src/
│   ├── components/
│   │   └── Navbar.jsx ──────────► Reusable navigation
│   │
│   ├── pages/
│   │   ├── Login.jsx ───────────► Authentication
│   │   ├── Register.jsx ────────► User registration
│   │   ├── Dashboard.jsx ───────► Main overview
│   │   ├── FeeManagement.jsx ───► Fee operations
│   │   └── AcademicRecords.jsx ─► Grades & attendance
│   │
│   ├── services/
│   │   └── api.js ──────────────► API integration
│   │
│   ├── utils/
│   │   └── AuthContext.jsx ─────► Auth state management
│   │
│   └── App.jsx ─────────────────► Main app & routing
```

### Admin Application Structure
```
admin-app/
│
├── src/
│   ├── components/
│   │   └── Navbar.jsx ──────────► Admin navigation
│   │
│   ├── pages/
│   │   ├── Login.jsx ───────────► Admin authentication
│   │   ├── Dashboard.jsx ───────► Statistics overview
│   │   ├── Users.jsx ───────────► User management
│   │   ├── Classes.jsx ─────────► Class management
│   │   └── Fees.jsx ────────────► Fee monitoring
│   │
│   ├── services/
│   │   └── api.js ──────────────► API integration
│   │
│   ├── utils/
│   │   └── AuthContext.jsx ─────► Auth state management
│   │
│   └── App.jsx ─────────────────► Main app & routing
```

## Data Flow

### Authentication Flow
```
User Input (Login Form)
        │
        ▼
    Validation
        │
        ▼
  API Call (POST /api/auth/login)
        │
        ▼
    Backend
        │
        ├─► Verify Credentials
        ├─► Check Device Verification
        └─► Generate JWT Token
        │
        ▼
   Response (Token + User Data)
        │
        ▼
  Store in localStorage
        │
        ▼
  Update Auth Context
        │
        ▼
  Redirect to Dashboard
```

### Fee Payment Flow
```
User Input (Amount)
        │
        ▼
    Validation
        │
        ▼
  API Call (POST /api/fees/deposit)
        │
        ├─► Headers: Authorization: Bearer <token>
        └─► Body: { amount: 50000 }
        │
        ▼
    Backend
        │
        ├─► Verify Token
        ├─► Validate Amount
        ├─► Update Balance
        └─► Create Transaction Record
        │
        ▼
   Response (Success/Error)
        │
        ▼
  Update UI
        │
        ├─► Show Success Message
        ├─► Refresh Balance
        └─► Update History
```

### Device Verification Flow
```
User Registers
        │
        ▼
  Device ID Generated
        │
        ▼
  Stored in Database (isVerified: false)
        │
        ▼
  User Cannot Login (Pending Verification)
        │
        ▼
  Admin Views User List
        │
        ▼
  Admin Clicks "Verify"
        │
        ▼
  API Call (POST /api/admin/users/:id/verify)
        │
        ▼
  Backend Updates (isVerified: true)
        │
        ▼
  User Can Now Login
```

## API Request/Response Format

### Request Format
```javascript
// Headers
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <jwt_token>"
}

// Body (example)
{
  "email": "student@example.com",
  "password": "password123"
}
```

### Response Format
```javascript
// Success
{
  "success": true,
  "data": {
    "user": { /* user data */ },
    "token": "jwt_token_here"
  },
  "message": "Login successful"
}

// Error
{
  "success": false,
  "message": "Invalid credentials"
}
```

## State Management

### Auth Context Structure
```javascript
AuthContext {
  user: {
    id: "user_id",
    name: "John Doe",
    email: "john@example.com",
    role: "student",
    isVerified: true
  },
  login: (userData, token) => { /* ... */ },
  logout: () => { /* ... */ }
}
```

### Component State Example
```javascript
// Dashboard Component
{
  feeBalance: 150000,
  recentPayments: [...],
  attendance: { percentage: 95 },
  loading: false,
  error: null
}
```

## Security Layers

```
┌─────────────────────────────────────┐
│  1. Frontend Validation             │
│     - Form validation               │
│     - Input sanitization            │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│  2. JWT Authentication              │
│     - Token in localStorage         │
│     - Auto-attach to requests       │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│  3. Protected Routes                │
│     - Check auth before render      │
│     - Redirect if unauthorized      │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│  4. Backend Verification            │
│     - Verify JWT token              │
│     - Check device verification     │
│     - Validate permissions          │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│  5. Database Security               │
│     - Hashed passwords (SHA-512)    │
│     - Encrypted sensitive data      │
└─────────────────────────────────────┘
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────┐
│                    PRODUCTION SETUP                       │
└──────────────────────────────────────────────────────────┘

┌─────────────────┐         ┌─────────────────┐
│  Client App     │         │  Admin App      │
│  (Vercel/       │         │  (Vercel/       │
│   Netlify)      │         │   Netlify)      │
│                 │         │                 │
│  Domain:        │         │  Domain:        │
│  app.school.com │         │  admin.school.  │
│                 │         │  com            │
└────────┬────────┘         └────────┬────────┘
         │                           │
         │    HTTPS Requests         │
         └───────────┬───────────────┘
                     │
         ┌───────────▼────────────┐
         │   Backend API          │
         │   (Heroku/AWS/         │
         │    DigitalOcean)       │
         │                        │
         │   Domain:              │
         │   api.school.com       │
         └───────────┬────────────┘
                     │
         ┌───────────▼────────────┐
         │   Database             │
         │   (MongoDB Atlas/      │
         │    AWS RDS)            │
         └────────────────────────┘
```

## Technology Stack Diagram

```
┌─────────────────────────────────────────────────────────┐
│                    FRONTEND STACK                        │
├─────────────────────────────────────────────────────────┤
│  UI Framework:        React 18                          │
│  Routing:             React Router v6                   │
│  HTTP Client:         Axios                             │
│  State Management:    Context API                       │
│  Build Tool:          Vite                              │
│  Styling:             Vanilla CSS                       │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    BACKEND STACK                         │
│                  (To be implemented)                     │
├─────────────────────────────────────────────────────────┤
│  Runtime:             Node.js                           │
│  Framework:           Express.js                        │
│  Authentication:      JWT + SHA-512                     │
│  Database:            MongoDB                           │
│  Security:            Helmet, Rate Limiting             │
│  Validation:          Express Validator                 │
└─────────────────────────────────────────────────────────┘
```

---

This architecture provides:
- ✅ Clear separation of concerns
- ✅ Scalable structure
- ✅ Security at multiple layers
- ✅ Easy to maintain and extend
- ✅ Production-ready design
